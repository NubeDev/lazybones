//! Extension registry.
//!
//! Loads extension `.wasm` components from the content-addressed blob store,
//! validates them, and indexes them by exported WIT interface so dispatch can
//! "find all gate-check extensions, invoke in order" (design §3.2, §3.5).
//!
//! SCAFFOLD: filled in by a later task.
